#!/bin/sh

if [ "1" = "$MUSS_TEST_UP_LOGS" ]; then
  echo log
  sleep 2 # wait long enough for status to be added
  exit 0
fi

echo std err >&2
echo "docker-compose"
for i in "$@"; do
  echo "$i"
done
